import headerStyles from '../src/utils/styles/Header.module.css';

const Header = () => {
  return (
    <div>
      <h1 className={headerStyles.title}>devFinder</h1>
    </div>
  );
};

export default Header;
